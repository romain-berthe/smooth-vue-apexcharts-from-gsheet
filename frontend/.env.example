# Frontend environment example
# Copy to `.env` and set the values.

# Mandatory: Backend base URL (used for auth/session + Sheets proxy)
VITE_BACKEND_URL=http://localhost:3001

# Mandatory: Google OAuth Web Client ID matching this frontend origin
VITE_GOOGLE_CLIENT_ID=YOUR_OAUTH_CLIENT_ID.apps.googleusercontent.com

# Sheets connector mode: backend | api | csv (backend recommended)
VITE_SHEETS_MODE=backend

# Optional: A1 range used by charts (applied to specific tabs by code)
VITE_SHEETS_RANGES=A1:Z100

# Optional (API mode only): direct Google Sheets access from browser
# VITE_SHEETS_ID=
# VITE_SHEETS_API_KEY=

# Optional (CSV mode only): published CSV URL
# VITE_SHEETS_CSV_URL=

# Optional: cache TTL in ms for frontend table/range caching (localStorage)
# VITE_SHEETS_CACHE_TTL_MS=300000
