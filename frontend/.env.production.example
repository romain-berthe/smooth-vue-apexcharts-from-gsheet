# Frontend environment example (production)
# Copy to `.env` for production builds and set the values.

# Mandatory: Backend base URL (public, HTTPS)
VITE_BACKEND_URL=https://api.kpis.example.com

# Mandatory: Google OAuth Web Client ID matching the production origin
VITE_GOOGLE_CLIENT_ID=YOUR_PROD_OAUTH_CLIENT_ID.apps.googleusercontent.com

# Sheets connector mode (backend recommended for production)
VITE_SHEETS_MODE=backend

# Optional: A1 range used by charts (applied to specific tabs by code)
VITE_SHEETS_RANGES=A1:Z200

# Optional (API mode only): direct Google Sheets access from browser
# VITE_SHEETS_ID=
# VITE_SHEETS_API_KEY=

# Optional (CSV mode only): published CSV URL
# VITE_SHEETS_CSV_URL=

# Optional: cache TTL in ms for frontend table/range caching (localStorage)
# VITE_SHEETS_CACHE_TTL_MS=300000
